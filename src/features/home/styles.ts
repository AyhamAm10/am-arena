import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    content: {
        paddingBottom: 16,
    },
    section: {
        marginTop: 16,
    },
    loaderSection: {
        marginTop: 24,
        paddingVertical: 24,
        alignItems: 'center',
    },
})
