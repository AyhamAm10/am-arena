/**
 * Messages and channels REST endpoints are not defined in the backend OpenAPI path
 * files merged in swaggerOptions (auth, friend, achievement, reel, pubg-tournament,
 * user, hero-content). No React Query hooks are generated for them until specifications exist.
 */
export {};
